import Controller.ReaderController;

public class FileParser {
    public static void main(String[] args) {
        ReaderController readerController = new ReaderController();
        readerController.walkFiles();
    }

}
